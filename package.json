{
  "name": "bloodalert",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:web": "turbo run dev --filter=web",
    "dev:linebot": "turbo run dev --filter=linebot",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "setup:python": "pip3 install Flask pony psycopg2-binary python-dotenv",
    "setup": "npm i yarn && yarn install && yarn run setup:python && docker-compose up -d --build"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "yarn": "^1.22.19"
  },
  "packageManager": "yarn@1.22.19"
}